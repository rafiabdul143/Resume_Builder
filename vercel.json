{
  "version": 2,
  "buildCommand": "cd ClientApp && npm install && npm run build",
  "outputDirectory": "wwwroot",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
